---
---

import { Heading, Subheading } from "@catalyst-ui-kit/typescript/heading";
import { Text, Strong, TextLink, Code } from "@catalyst-ui-kit/typescript/text";
import { Divider } from "@catalyst-ui-kit/typescript/divider";

<Heading level={1}>Git基礎1：なぜ必要なのか？</Heading>
<Text>
  このページではコマンドを一切使わずに、Git がどんな場面で役に立つのかをイメージで理解します。AI 開発でよくある「モデルやスクリプトが急に壊れた」「AI エージェントが予想外の修正を入れた」といった状況でも、安心して戻れる準備を整えることがゴールです。
</Text>
<div className="mt-4 inline-flex items-center gap-1.5 rounded-full bg-emerald-50 px-2.5 py-1 text-xs/5 font-semibold text-emerald-800 ring-1 ring-emerald-200 dark:bg-emerald-400/10 dark:text-emerald-200 dark:ring-emerald-300/30">
  <span className="tracking-wide">更新日: 2025/09/22</span>
</div>

<Divider soft className="my-10" />

<Subheading level={2}>この記事でわかること</Subheading>
<ul className="mt-4 space-y-3 text-base/7 text-gray-700 dark:text-gray-300">
  <li>Git が解決する問題と「履歴管理ツール」としての役割</li>
  <li>AI 開発で Git を使うメリット（生成AIとの共存を含む）</li>
  <li>AI に Git 操作を任せる前に理解しておきたいチェックポイント</li>
  <li>実務でありがちな失敗と、Git がどうリカバリーを助けるか</li>
</ul>

<Divider soft className="my-10" />

<Subheading level={2}>Git が解決する課題</Subheading>
<Text className="mt-3">
  チームや個人でコードや資料を更新していると、「いつ」「誰が」「なぜ」この変更をしたのかを忘れてしまう瞬間が必ず訪れます。コピーを手動で残しておくやり方では、ファイル名がごちゃごちゃになり、必要なときに探し出せません。Git は変更の履歴を一覧でき、好きな時点に戻せるようにすることで、この混乱を防ぎます。
</Text>
<div className="mt-3 analogy-card" role="note">
  <p className="leading-6">たとえ: RPG のセーブポイントを思い出してください。難所に挑む前にセーブしておけば、負けても直前からやり直せます。Git はファイルの世界にセーブポイントを置いてくれるシステムです。</p>
</div>

<Subheading level={2} className="mt-12">AI 開発と Git の相性</Subheading>
<Text className="mt-3">
  生成AIやオートメーションツールを使うと、数分で大量のコードや設定が書き換わります。便利な一方、<Strong>自分が意図しない変更</Strong>が紛れ込むリスクも高まります。Git でこまめに履歴を残しておけば、AI が提案した修正を検証しながら採用でき、必要なら数クリックで元に戻せます。
</Text>
<div className="mt-3 analogy-card" role="note">
  <p className="leading-6">たとえ: AI を頼れる仲間キャラだと思っても、ときには意図しないスキルを使ってしまいます。Git でこまめにセーブしておけば「最後の安全なセーブデータ」に戻れるので、安心して任務を続けられます。</p>
</div>

<Text className="mt-4">
  Git はブランチという「実験用の分岐」を簡単に扱えます。AI モデルのパラメータを大胆に変えるときでも、別の枝で試せば本番コードに影響を与えません。うまくいった案だけをメインに取り込むことで、速度と安全性を両立できます。
</Text>

<Subheading level={2} className="mt-12">AI に任せても理解が必要な理由</Subheading>
<Text className="mt-3">
  近年は AI エージェントに <Term definition="AI に伝える指示文。相手が理解しやすいよう状況や目的を整理して伝える。">プロンプト</Term> を送るだけで、<Code>git add</Code> や <Code>git commit</Code>、<Code>git push</Code> まで代行させることができます。このシリーズの最後では、実際に AI に任せるための指示テンプレートも紹介します。
</Text>
<Text className="mt-3">
  ただし、<Strong>内部で何が行われているのかを自分が理解していないと、想定外の差分やトラブルに気づけません</Strong>。どこまでが安全な変更かを判断するのは人間の役割です。基礎を押さえておけば、AI が示すログやエラーメッセージを読み解き、必要なときに手動でフォローできます。
</Text>
<div className="mt-3 analogy-card" role="note">
  <p className="leading-6">たとえ: 自動運転の車でも、道路標識やブレーキの仕組みをざっくり知っているドライバーであれば、非常時に安全に介入できます。Git の基本も同じで、AI に任せる前に土台を理解しておくことで安心して任せられます。</p>
</div>

<Subheading level={2} className="mt-12">ありがちな失敗と回避策</Subheading>
<ul className="mt-4 space-y-3 text-base/7 text-gray-700 dark:text-gray-300">
  <li>
    <Strong>「昨日の動く状態に戻れない」</Strong> — Git は細かい単位で履歴を残せるので、直前の状態だけでなく、安定していた時点まで遡れます。
  </li>
  <li>
    <Strong>「AI が書き換えた差分が読み切れない」</Strong> — 変更点が視覚化されるため、採用する部分と差し戻す部分を明確に判断できます。
  </li>
  <li>
    <Strong>「複数人の作業がぶつかる」</Strong> — 履歴が基準になるため、誰が何を変えたかがすぐに分かり、トラブルの切り分けが容易です。
  </li>
</ul>

<Divider soft className="my-10" />

<Subheading level={2}>次に進む</Subheading>
<ul className="mt-4 space-y-3 text-base/7 text-gray-700 dark:text-gray-300">
  <li>
    <Strong>Git基礎2：インストールと初期設定</Strong>（次の記事）：<TextLink href="/git-install">環境構築の手順</TextLink>を確認する
  </li>
  <li>
    <Strong>Git基礎3：変更を記録する超基礎サイクル</Strong>：履歴を残す操作を体験する
  </li>
  <li>
    <Strong>Git AIアシスト編：AI に Git コマンドを任せるプロンプト術</Strong>：<TextLink href="/git-ai-automation">AI に依頼するときのポイント</TextLink>を学ぶ
  </li>
</ul>

<Text className="mt-6">
  Git の考え方さえ掴んでおけば、コマンドや GUI 操作は自然と理解できます。次のステップでは、自分の環境に Git を導入して準備を整えましょう。
</Text>
